html
    head
        meta(charset="utf-8")
        meta(http-quiv="X-UA-Compatible", content="IE=edge,chrome=1")
        meta(name="view-port", content="width=device-width, initial-scale=1.0, maximum-scale=1.0")
        meta(name="description", content="在线体验 k-means 聚类分析算法.")
        title!= title
        link(rel="shortcut icon", href="#{cdn.cdnify('favicon.ico')}")
        link(rel="stylesheet", type="text/css", href="#{cdn.cdnify('semantic/dist/semantic.min.css')}")
        link(rel="stylesheet", type="text/css", href="#{cdn.cdnify('bower_components/handsontable/dist/handsontable.full.min.css')}")
        style(type="text/css").
            .cmd {
                -moz-border-radius: 5px;
                -webkit-border-radius: 5px;
                background-color: #202020;
                border: 4px solid silver;
                border-radius: 5px;
                box-shadow: 2px 2px 3px #6e6e6e;
                color: #e2e2e2;
                display: block;
                font: 1em 'andale mono', 'lucida console', monospace;
                line-height: 1em;
                overflow: auto;
                padding: 15px;
            }

            .auto-wrap {
                word-wrap: normal;
                white-space: pre-wrap;
            }

            .hard.auto-wrap {
                word-wrap: break-word;
            }

body
    div(ng-app="kMeansModule", ng-controller="kMeansCtrl")
        div.ui.two.column.grid
            div.ui.column
                include partial/dash
            div.ui.column
                include partial/k-means

    div
        script(src="#{cdn.cdnify('bower_components/jquery/dist/jquery.min.js')}")
        script(src="#{cdn.cdnify('semantic/dist/semantic.min.js')}")
        script(src="#{cdn.cdnify('bower_components/Flot/jquery.flot.js')}")
        script(src="#{cdn.cdnify('bower_components/Flot/jquery.flot.navigate.js')}")
        script(src="#{cdn.cdnify('bower_components/Flot/jquery.flot.symbol.js')}")
        script(src="#{cdn.cdnify('bower_components/angular/angular.min.js')}")
        script(src="#{cdn.cdnify('scripts/kMeansModule.js')}")
        script(src="#{cdn.cdnify('bower_components/handsontable/dist/handsontable.full.min.js')}")
        script(type="text/javascript").